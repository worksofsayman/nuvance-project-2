'use client';
import ContributingPage from '../../components/ContributingOverlay';
import RoadMap from '../../components/ContributingOverlay';

export default function Roadmap() {
  return <ContributingPage />;
}